/**
 * --------------------------------------------------------------------
 * Fichier de Traduction pour Framer : NE PAS UTILISER EN PRODUCTION
 * --------------------------------------------------------------------
 *
 * Ce fichier est une version traduite du composant original
 * (`05-component-bloc.jsx`) destinée à être copiée-collée
 * dans un Code Component de Framer.
 *
 * Il convertit les classes Tailwind CSS en objets de style inline.
 * Les styles responsives (ex: `md:`) sont laissés en commentaires
 * pour être appliqués via l'interface de Framer.
 *
 * @see 05-component-bloc.jsx
 */

import React from 'react';
import { motion } from 'framer-motion';

// Le nom du composant sera la version "Framer" de l'original
const ComponentNameFramer = ({ text, className }) => {
  // Le contenu de ce composant est une traduction de l'original.

  return (
    <div style={{
      /* Les styles de base convertis viendront ici */
    }}>
      {/* 
        NOTE POUR FRAMER (Styles Responsives): 
        - Pour Tablet (md): appliquer la propriété X...
        - Pour Desktop (lg): appliquer la propriété Y...
      */}
      {text}
    </div>
  );
};

export default ComponentNameFramer; 